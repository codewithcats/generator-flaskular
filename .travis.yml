language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-flaskular' ]; then cd .. && eval "mv $currentfolder generator-flaskular" && cd generator-flaskular; fi

